# Inner LuCI package Makefile — included by the outer repo Makefile.

include $(TOPDIR)/rules.mk

LUCI_PKGARCH:=all
LUCI_DEPENDS:=+luci-base

include ../../luci.mk

# Build nothing — assets are pre-baked.